import 'package:flutter/cupertino.dart';

class Dimens {
  static const pageHorizontalPadding = 16.0;
  static const pageVerticalPadding = 16.0;

  static const contentPadding = EdgeInsets.symmetric(
      horizontal: Dimens.pageHorizontalPadding,
      vertical: Dimens.pageVerticalPadding);
}

const dp8x1 = 8.0;
const dp8x2 = 8.0;

const gh8x1 = SizedBox(height: dp8x1);
const gh8x2 = SizedBox(height: dp8x2);
